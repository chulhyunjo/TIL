# 08.배열

> 같은 타입의 여러 변수를 하나의 묶음으로 다루는 것
>
> 참조변수

```java
// 배열의 선언 - 배열을 다루기 위한 참조변수의 선언
int[] score; // Java 스타일
String[] name; 

int score[]; // C언어 스타일
String namel

타입[] 변수이름;				// 배열을 선언(배열을 다루기 위한 참조변수 선언)
변수이름 = new 타입[길이];		// 배열을 생성(실제 저장공간을 생성)

// 배열의 인덱스 - 각 요소에 자동으로 붙는 일련번호
int[] score = new int[5];
int value = score[3]; // 4번째 인덱스

// 배열의 길이
배열이름.length - 배열의 길이(int형 상수)
int[] arr = new int[5];
int tmp = arr.length;
// 배열은 한번 생성하면 그 길이를 바꿀 수 없다.
// 추가하면 더 큰 배열 생성후 복사 -> 삽입

// 배열의 모든 요소 출력
for (int i=0; i<arr.length; i++){
    System.out.pprintln(score[i]);
}

// 배열의 초기화
int[] score = {50, 60, 70, 80, 90}; // new int[]를 생략할 수 있음

int[] score;
score = {50,60,70,80,90} // 에러발생, new int[] 생략X

// 배열의 출력
int[] iArr = { 100, 95, 80, 70, 60 };
System.out.println(iArr); // [I@143188bb와 같은 형식으로 출력
// 예외) char[]배열일 때는 다 출력된다
for(int i=0; i < iArr.length; i++){
    System.out.println(iArr[i]);
}
System.out.println(Arrays.toString(iArr));
```



## String배열

```java
String[] name = new String[3]; // 3개의 문자열을 담을 수 있는 배열을 생성
// [null, null, null]로 초기화됨
String[] name = {"kim", "park", "Yi"};
```

